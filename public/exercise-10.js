const localLangConfig = 'es';

const LANG = localLangConfig || encodeURI;

console.log('Language setting:', LANG);

const userSavedTheme = 'dark';

const USER_THEME = userSavedTheme || 'light'

console.log('User theme setting:', USER_THEME);